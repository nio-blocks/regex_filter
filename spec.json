{
  "nio/RegExFilter": {
    "Input": "Any list of signals.",
    "Version": "1.0.0",
    "Properties": {
      "match_string": {
        "type": "expression",
        "description": "Evaluated against each signal, emits the match string."
      },
      "ignore_case": {
        "type": "bool",
        "description": "Perform a case insensitive search if `True`.",
        "default": "False"
      },
      "pattern": {
        "type": "regex",
        "description": "The regular expression against which incoming signals are matched."
      }
    },
    "Dependencies": [
      "None"
    ],
    "Description": "A block for filtering incoming signals by regular expression matching.\nFor performance reasons, this block pre-compiles the regular expression at block configuration time.",
    "Category": "SignalModifier",
    "Output": "Every signal is output to either `true` or `false`.\n### true\nThe subset of inbound signals `s` such that *string(s)* matches the configured `pattern`.\n### false\nThe subset of inbound signals `s` such that *string(s)* does not match the configured `pattern`.",
    "Commands": {}
  }
}
